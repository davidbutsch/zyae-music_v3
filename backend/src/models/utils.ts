export const reqString = {
  type: String,
  required: true,
};

export const uniqueReqString = {
  type: String,
  required: true,
  unique: true,
};

export const reqNum = {
  type: Number,
  required: true,
};

export const reqBool = {
  type: Boolean,
  required: true,
};
